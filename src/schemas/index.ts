import category from './category';
import product from './product';
import other from './other';
import hero from './hero';

const schemaTypes = [category, product, other, hero];

export default schemaTypes;
